# ============================================================
# frontend/.env  — set BEFORE running `npm run build`
# ============================================================
# Public base URL of the site (same origin as the API is served from).
# The frontend calls `${REACT_APP_BACKEND_URL}/api/...`
REACT_APP_BACKEND_URL=https://your-domain.com
